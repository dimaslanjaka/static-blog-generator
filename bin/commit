#!/usr/bin/env node
"use strict";

const path = require("path");
const minimist = require("minimist")(process.argv.slice(2));
console.dir(minimist);

const files = [];
if (typeof minimist.file == "string") {
  minimist.file.split(",").forEach((f) => {
    console.log(f);
  });
}

//git commit -m $*
