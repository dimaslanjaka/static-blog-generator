export declare function uniqueArray<T extends any[]>(array: T): T;
/**
 * Unique Array Of Strings
 * @description Lowercase all string and filter duplicated from them
 * @param arr
 * @returns
 */
export declare function uniqueStringArray(arr: Array<string>): string[];
export default uniqueArray;
